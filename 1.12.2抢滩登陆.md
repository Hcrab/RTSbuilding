# RTSBuilding 1.12.2 抢滩登陆

> 任务性质：无人值守的全量移植攻坚，不是可行性原型。  
> 源版本：`E:\RTSbuilding` 的 Minecraft 1.21.1 / NeoForge `main`。  
> 目标版本：Minecraft 1.12.2 / Forge 14.23.5.2847。  
> 总策略：先强搬全部生产代码、资源和测试，再按编译错误与运行阶段逐层完成版本本地化。  
> 授权：允许建立独立 Git 工作树和本地分支；允许联网下载 JDK、Gradle、Forge、映射、依赖、测试运行时和必要的开源参考源码；允许长时间编译、启动客户端/专用服务端并运行自动化测试。  
> 无人值守原则：能由 agent 判断、实现、编译或自动验证的事项不得等待人类；只有真正需要视觉、手感、外部账号、真人多人协作或产品取舍的事项才挂账。

## 1. 最终目标

把当前 1.21.1 RTSBuilding 全量移植到 1.12.2，形成一个可持续开发、可构建、可启动、可测试、最终可发布的 Forge 版本。

“全量”包括但不限于：

- RTS 模式进入、退出、状态恢复与鼠标捕获；
- 相机移动、旋转、缩放、边缘滚动、平滑与多人同步语义；
- 顶栏、底栏、浮窗、设置、教程、搜索、提示和四语言资源；
- 单方块/范围放置与破坏、快速建造、连锁挖掘、创造模式行为；
- 服务端任务引擎、预算、批处理、持久化、断线和重进恢复；
- 撤回/重做、历史记录、掉落捕获与物品真实性；
- 玩家物品栏、箱子、双箱、工具、耐久、NBT 和能力储存；
- 蓝图捕获、保存、导入、搜索、预览、旋转、固定与建造；
- 网络安全、权限、距离、维度、任务所有权和专用服务端安全；
- 兼容层、资源、配方、标签、模型、纹理、声音和发布元数据；
- 现有 unit、contract、performance、UI Core/Kit 与 GameTest 行为契约。

不能为了编译通过而永久删除功能、排除包、注释入口、返回假成功、吞掉异常或用空实现冒充完成。临时桩只能用于解除编译依赖，必须带清晰 TODO、集中登记，并在相应阶段结束前替换。

## 2. 工作区、分支与权限

### 2.1 隔离红线

根工作区 `E:\RTSbuilding` 当前可能包含用户或其他 agent 的未提交工作。它是只读源和对照物，禁止在其中回退、清理、暂存、提交或覆盖任何现有改动。

新建独立工作树：

```text
工作树：E:\RTSbuilding-1.12.2-forge
本地分支：codex/port-1.12.2
起点：启动时的 origin/main（记录准确 SHA）
```

如果路径或分支已经存在，先用只读命令确认其归属和状态；不得删除未知工作。发生冲突时改用唯一后缀路径和分支，并记录实际值。

允许在目标工作树中：

- 修改、移动、新增和删除属于 1.12.2 移植的文件；
- 下载并缓存构建依赖；
- 创建目标工作树专用 Gradle 缓存；
- 运行构建、测试、客户端、专用服务端及生成运行文件；
- 做小而完整的本地提交，确保任一阶段可回退；
- 使用小 agent 并行处理互不重叠的模块。

未经人类明确要求，不得 push、不得创建公开 PR、不得改动 `main` 或任何长期远程分支。

### 2.2 源版本冻结规则

启动时记录：

- `origin/main` SHA；
- 根工作区 `HEAD` SHA；
- 根工作区已修改和未跟踪的生产/测试文件清单；
- 目标 ForgeDevEnv、RFG、MixinBooter 和映射版本。

第一轮以已提交的 `origin/main` 为可复现基线。根工作区的未提交文件只允许只读分析，不得整包复制进目标工作树，因为它们可能仍处于其他任务中。将这些路径登记为“待源分支提交后补同步”；若其代码显然是完成且移植不可缺失，可以提取设计语义，但不得反向触碰根工作区。

## 3. 现代 1.12.2 开发环境

优先以 CleanroomMC 的 `ForgeDevEnv` 为构建地基，但发布产物保持普通 Forge 兼容：

```text
Minecraft              1.12.2
Forge API              14.23.5.2847
Gradle                 9.6.1（或模板当时锁定的更新兼容版）
RetroFuturaGradle      2.0.2（或模板当时锁定的更新兼容版）
Gradle JVM             Java 25
发布字节码             Java 8
Mixin                  MixinBooter 11 / 模板当前兼容版本
映射                   MCP stable_39，除非依赖验证要求调整
```

环境建立要求：

1. 先检查本机已有 JDK；缺少合适 Java 25 时允许下载安装可信发行版。
2. 允许从官方 GitHub、Forge Maven、Cleanroom Maven、GTNH Maven、Maven Central 和模组官方 Maven 下载依赖。
3. 不复用或改写 `sister-projects` 中的旧 1.12.2 代码作为产品基线；那只能作为只读考古参考。
4. 先验证 `gradlew tasks`、依赖解析、反编译/映射和最小示例构建，再强搬主项目。
5. Gradle 用 Java 25 不代表模组只能运行在 Java 25；默认保持 Java 8 发布字节码，使同一 JAR 覆盖 Classic Forge 和 Cleanroom。

目标运行矩阵：

```text
同一个 RTSBuilding 1.12.2 JAR
├─ Classic Forge 14.23.5.2847 + Java 8
└─ Cleanroom 当前推荐版 + 其要求的现代 Java
```

不得直接依赖 Cleanroom 专属 API，除非使用反射/可选适配器隔离，并证明 Classic Forge 不会类加载失败。

## 4. Agent 作战方式

### 4.1 横冲直撞，但不互相踩文件

主 agent 是唯一整合者，负责环境、阶段推进、编译、运行测试、提交和本文档更新。小 agent 可以并行猛写，但任务必须按目录或职责划分，例如：

- 构建、注册、配置、资源和入口；
- 公共纯 Java、UI Core/Kit 与单元测试；
- 服务端任务、放置、破坏、挖掘、储存和持久化；
- 网络、客户端输入、相机、GUI 与世界渲染；
- 蓝图、兼容层、测试迁移与运行时诊断。

同一时刻不得让两个 agent 修改同一组文件。小 agent 完成后交给主 agent 审查、编译和整合。不要为等待一个困难模块而闲置：把阻塞登记后立即切换到下一个可推进模块。

### 4.2 机械迁移优先

推荐循环：

```text
强搬完整目录
→ 编译
→ 对错误按同类 API 聚类
→ 批量建立 1.12.2 等价实现或适配层
→ 再编译
→ 到达下一个生命周期阶段
→ 运行自动化验证
→ 小提交
→ 继续下一层
```

优先做可机械化的改名、包迁移、签名变化、Codec/NBT 替换、注册器和事件转换。遇到 API 不存在时，先理解 1.21.1 代码表达的业务语义，再寻找 1.12.2 等价实现；不得只按类名猜测。

## 5. 分阶段推进

### 阶段 0：建立地基

- 创建隔离工作树和本地分支；
- 导入当前 ForgeDevEnv；
- 确认 Java 25、Gradle、RFG、Forge、Mappings、Mixin 可解析；
- 构建模板并启动空白 `runClient`、`runServer`；
- 记录所有版本、SHA、下载源和命令。

退出条件：模板构建成功，Forge 客户端和专用服务端至少到达正常加载阶段。

### 阶段 1：全量强搬

- 搬入 1.21.1 的全部生产 Java、资源和测试源；
- 保留包结构和业务职责，先不要趁移植重做产品架构；
- 把 NeoForge 元数据换为 1.12.2 Forge 元数据；
- 生成第一份完整编译错误台账，按构建、语言、注册、数据、网络、服务端、客户端、渲染、兼容和测试分类；
- 对 Java 8 不支持的 `record`、模式匹配、现代集合工厂和语言特性做机械等价改写；若模板的 Jabel 模式可靠，可用于语法便利，但不得破坏 Java 8 字节码底线。

退出条件：所有源文件都已进入目标树，没有通过永久排除目录来伪造进度。

### 阶段 2：公共逻辑和数据结构

- 优先救活与 Minecraft/NeoForge 脱钩的 UI Core、UI Kit、算法、布局、状态机和纯数据代码；
- 将现代 `ResourceLocation`、方块状态、坐标、方向、物品栈、NBT 和序列化调用迁移到 1.12.2 语义；
- 现代 Codec/StreamCodec/DataComponent 改为明确的 NBT/ByteBuf/版本化读写；
- 保持任务 ID、UUID、维度、坐标和存档兼容语义，不因旧 API 麻烦而丢字段。

退出条件：纯 Java 测试和可隔离的业务测试通过，公共承重结构能够编译。

### 阶段 3：Forge 生命周期与服务端

- 完成 `@Mod` 入口、代理/side 隔离、事件总线、注册、配置、命令、tick、玩家登录退出和世界生命周期；
- 恢复 placement/destruction/mining 任务引擎、预算和批处理；
- 使用 1.12.2 capability 与真实 `ItemStack` 实现储存、工具、耐久、NBT 变异和剩余物退回；
- 恢复持久化、掉落捕获、断线恢复、维度卸载和专用服务端类加载安全。

退出条件：`compileJava` 通过；专用服务端能加载模组并建立测试世界；核心服务端行为有自动化覆盖。

### 阶段 4：网络

- 将现代 payload/codec/registrar 迁为 1.12.2 `SimpleNetworkWrapper` 或等价可靠实现；
- 保留客户端意图与服务端权威验证边界；
- 每个包验证发送方向、玩家、维度、距离、权限、数量、任务所有权和线程切换；
- 禁止客户端坐标或数量未经检查直接执行；
- 客户端专属消息处理类不能在专用服务端被类加载。

退出条件：所有消息可注册和编解码，测试证明线程调度与拒绝路径正确，专用服务端启动不触发客户端类。

### 阶段 5：客户端输入、相机与 GUI

- 迁移按键、鼠标、滚轮、拖动、输入所有权和原生鼠标恢复；
- 将现代 `GuiGraphics`、scissor、字体、纹理和 Screen 入口映射到 1.12.2 GUI；
- 尽量保留 UI Core/Kit 的状态、布局、主题和命中测试，只重写 Minecraft 平台 Canvas/输入适配器；
- 保持浮窗层级、无文字阴影穿透、编辑框内边距、滚轮不泄漏相机等现有产品约束；
- 恢复 RTS 相机进入/退出、WASD、旋转、缩放和平滑。

退出条件：客户端进入世界且能开关 RTS；主要 UI 路由无异常；可自动验证的布局和输入契约全部进入测试。

### 阶段 6：世界渲染与蓝图

- 用 1.12.2 世界渲染事件和 OpenGL/LWJGL 2 路径重写选择框、虚影、方块预览和覆盖层适配器；
- 不依赖 OptiFine 私有实现，不以 OptiFine 为架构中心；
- 首先保证原版 Forge 渲染正确，再测试 Nothirium、Celeritas/Cleanroom，OptiFine 只作为存量兼容烟测；
- 恢复蓝图捕获、文件、搜索、预览、旋转、固定、材料计算和服务端建造；
- 自有渲染不得错误 flush/end Minecraft 共享缓冲，状态恢复必须明确。

退出条件：蓝图完整闭环可自动执行；渲染路径不崩溃、不污染后续世界和 GUI 绘制。

### 阶段 7：兼容、资源与全矩阵

- 恢复四语言、纹理、模型、配方、声音、教程和设置参考；
- 第三方兼容按“存在则启用、缺失不崩溃”实现；暂时拿不到 1.12.2 依赖时先隔离可选适配器，不能阻塞核心模组；
- 构建发布 JAR，并在 Classic Forge 与 Cleanroom 使用同一个 JAR；
- 跑客户端、专用服务端、自动化行为、存档重载和依赖缺失矩阵。

退出条件：没有为通过构建而删除的核心功能；已知自动化失败为零；人工项全部集中登记。

## 6. 1.12.2 的 GameTest 策略

1.12.2 没有现代原生 GameTest 框架，所以“跑 GameTest”指迁移行为契约，而不是执着于同名 API。

按优先级建立三层测试：

1. **纯 JVM 测试**：迁移现有 unit、contract、performance、UI Core/Kit 测试。
2. **Forge 集成测试模组/测试命令**：在测试世界中确定性执行放置、破坏、挖掘、储存、NBT、网络拒绝、任务恢复和蓝图断言；失败时让进程返回非零或产生机器可判定报告。
3. **运行时烟测**：自动启动 Classic Forge 专用服务端和客户端测试实例，检查模组加载、世界进入、测试完成标记、异常和服务端客户端类加载泄漏。

必须尽量逐项映射当前 `RtsServerGameTests`，不得简单删除 `gametest` 包后宣称完成。某项确实只能依赖真人输入或视觉判断时，先用状态/截图/日志/像素或测试接口覆盖能自动证明的部分，只把剩余主观部分挂账。

每轮验证至少保存：命令、环境、退出码、通过/失败数量、首个根因和日志路径。长日志不要提交进源码仓库。

## 7. 需要人类验证的统一挂账规则

不要因为出现人工项而停止。所有人工项只追加到本文档末尾的“无人值守执行总报告”，格式必须包含：

```text
[HUMAN-编号] 简短标题
- 已由机器证明：
- 仍需人类判断：
- 最短复现步骤：
- 期望结果：
- 当前构建/提交：
- 截图或日志路径：
- 不阻塞继续开发的理由：
```

允许挂账的典型事项：

- 真正的相机和拖拽手感；
- 中英简繁字体、IME 和高缩放视觉；
- 不同 GPU、OptiFine/Nothirium/Celeritas 的真实画面；
- 需要两个真人账号的多人交互；
- 需要维护者决定的兼容承诺或产品取舍。

不允许挂账的事项：编译错误、可读日志、类加载异常、确定性状态转换、序列化往返、网络方向、空指针、资源缺失和能由测试世界验证的方块/物品结果。

## 8. 提交与进度纪律

- 每个阶段或同类 API 批次形成小型本地提交；
- 提交前至少运行 `git diff --check` 和该批次最相关的测试；
- 构建红时允许提交明确标记的机械搬运检查点，但下一提交必须继续减少已记录错误，不能长期停在假绿状态；
- 提交信息清楚描述恢复了什么，不用“misc fixes”；
- 不提交下载缓存、运行目录、日志、反编译产物、账号信息或密钥；
- 不推远程；不修改根工作区；不覆盖其他 agent 的工作树；
- 定期把准确 SHA、通过数、剩余错误类别和下一步追加到本文档总报告。

## 9. 无人值守停止条件

除以下情况外不得停下来等人：

1. 磁盘空间不足且清理任何目标都可能误删用户数据；
2. 所有可信依赖源持续不可达，且本机没有缓存或可替代版本；
3. 需要凭据、付费资源、许可证授权或外部账号；
4. 根工作区的未提交实现是继续推进所必需，但其状态无法安全判断；
5. 连续尝试多个技术路径后确认必须由维护者作产品取舍。

即使遇到这些情况，也要先完成其余独立模块、保存本地提交、整理证据，再在总报告中写明唯一阻塞点。

## 10. 完成定义

最低可交付不是“工程能编译”，而是：

- 同一 JAR 能在 Classic Forge 14.23.5.2847 + Java 8 加载；
- 同一 JAR 能在 Cleanroom 当前推荐环境加载；
- 客户端和专用服务端均能启动并进入测试阶段；
- 自动化行为契约、资源检查和序列化往返全部通过；
- RTS 核心玩法、任务、网络、储存、蓝图、UI 和渲染都存在真实实现；
- 没有通过排除源码、永久空桩或静默禁用功能获得的假绿；
- 所有只能由人类完成的验证已集中在本文档，不散落在聊天、TODO 或多个临时文件里；
- 工作树状态、提交链、构建命令、产物路径和剩余风险足够下一位 agent 无需重新考古即可继续。

---

## 11. 无人值守执行总报告

> 本节由执行 agent 持续追加。不要重写上面的任务边界；只记录事实、提交、测试、阻塞和人工验证。

### 启动快照

- 状态：2026-07-28 已启动无人值守执行；根工作区保持只读，目标工作树已建立。
- 源 `origin/main` SHA：`d578c13101ccde6bf572d61fc7f52be48788a2e6`。
- 根工作区启动 `HEAD`：`d578c13101ccde6bf572d61fc7f52be48788a2e6`。
- 目标工作树/分支：`E:\RTSbuilding-1.12.2-forge` / `codex/port-1.12.2`，从上述 `origin/main` 创建，未 push。
- ForgeDevEnv：`CleanroomMC/ForgeDevEnv@39d8e3b8736ba605f19a80b6d01df2ee01d42476`（2026-07-25）。
- 环境版本：Gradle `9.6.1`、RetroFuturaGradle `2.0.2`、Forge `14.23.5.2847`、MCP `stable_39`、MixinBooter `11.0`、Gradle JVM Microsoft OpenJDK `25.0.3`、发布字节码 Java 8。
- 下载源：ForgeDevEnv GitHub、Gradle GitHub Releases、GTNH Maven、Cleanroom Maven、Forge/Minecraft 官方依赖源与 Maven Central。
- 源规模：生产 Java `807` 个、测试 Java `204` 个、主资源 `154` 个；所有已提交文件随工作树完整进入目标树。
- 根工作区已修改生产/测试文件：`client/network/RtsClientNetworkHandlers.java`、`RtsClientPacketGateway.java`、`client/screen/handler/PlacementHistoryManager.java`、`ScreenShapeController.java`、`client/screen/standalone/BuilderScreenKeyboardActionOwner.java`、`common/RtsHistoryConstants.java`、`gametest/RtsServerGameTests.java`、`network/builder/RtsBuilderPackets.java`、`S2CRtsHistorySyncPayload.java`、`handler/RtsInteractionHandlers.java`、`server/history/HistoryBlockRecord.java`、`HistoryEntry.java`、`HistoryExecutor.java`、`ServerHistoryManager.java`、`server/pipeline/mining/MiningExecutePipe.java`、`server/pipeline/sync/HistoryRecordPipe.java`、`server/service/destruction/RtsDestructionBatch.java`、`server/service/mining/MultiBlockTracker.java`、`RtsMiningStateMachine.java`、`RtsUltimineProcessor.java`、`server/service/placement/RtsPlacementBatch.java`、`server/task/RtsDurableTaskExecutionRuntime.java`、`RtsTaskEngine.java`、三类 task codec/state、四语言文件及四个 task 单元/契约测试；均未复制，登记为待源分支提交后补同步。
- 根工作区未跟踪生产/测试文件：`network/builder/C2SRtsRedoPayload.java`、`server/history/HistoryCapacityPolicy.java`、`HistoryExecutionResult.java`、`HistoryOperation.java`、`src/test/unit/.../server/undo/`；均未复制，登记为待源分支提交后补同步。

### 已完成里程碑

- 阶段 0：隔离工作树和本地分支已建立；ForgeDevEnv 构建脚手架已导入，正在验证依赖与空白运行时。
- 阶段 0：`gradle tasks` 已绿色，RFG/Forge/MCP 配置完成解析；自动获取了 Classic Forge 验证用 Azul Java `8u502`。
- 阶段 1：`origin/main` 的完整生产代码、资源、测试源已天然随工作树进入目标树；未通过排除目录伪造进度。
- 阶段 1 资源本地化首批：生成四语言 `.lang`（每份 1026 行）、17 个 1.12 Forge recipe、`textures/items` 兼容路径，并保留现代原资源供语义对照。
- 阶段 2 UI Core/Kit：确认当前 main 已经完全保持 Java 8 运行库语义，无需源码改写；生产 214 文件和测试 121 文件全部脱离 Minecraft/Forge/LWJGL。
- 阶段 2 服务端首批：核心 task ID、等待键、快照/墓碑、slice/result、pipeline result/key 已迁为 Java 8 类；新增 1.12 维度/NBT 兼容边界，三类任务 state/codec 与 history 开始落到真实 1.12 API。

### 当前自动化结果

- `Java 25.0.3`：通过。
- `Java 8u502` toolchain 自动解析/启动：通过。
- `git ls-remote ForgeDevEnv`：通过，模板 SHA `39d8e3b...`。
- ForgeDevEnv `tasks`（Gradle 9.2.1 兼容运行、RFG 2.0.2）：通过，耗时 2 分 38 秒。
- UI Core/Kit Java 8 验证：生产 `214/214` 文件编译通过（388 class），测试 `121/121` 文件编译通过（158 class），JUnit `414/414` 通过、0 skip、0 fail；合计 546 class 均为 major 52。
- UI Core/Kit 隔离扫描：0 个 Minecraft/Forge/NeoForge/LWJGL import，0 个已知 Java 9+ 运行库 API。
- Gradle Wrapper 首次从 `services.gradle.org` 下载两次连接超时；已改用同一官方 Gradle GitHub Release 直链并继续重试，尚不构成停止条件。

### 剩余机器任务

- 完成模板 `tasks/build/runClient/runServer` 验证；生成第一轮完整编译错误分类台账；并行迁移 UI Core/Kit、服务端、网络/客户端与资源测试。

### 需要人类验证

- 暂无；当前没有阻塞机器继续推进的人工项。

### 最终交接

- 最后提交：待填写。
- 构建产物：待填写。
- 复现命令：待填写。
- 已知风险：待填写。

### 2026-07-28 环境基线闭环与整包编译前沿

- ForgeDevEnv 空模板已完成真实闭环：`build` 成功（18 个任务）；样例模组 class major 为 52；Classic Forge 14.23.5.2847 + Java 8 专用服务端成功加载 6 个模组、生成世界并到达 `Done`，随后正常保存并退出；客户端成功初始化 LWJGL/OpenGL/声音/纹理图集、加载样例模组并到达主菜单。此结果关闭阶段 0，不再把环境问题混入项目代码错误。
- 目标工作树的 RFG 任务图已成功解析；UI Core/Kit 已通过目标 Gradle 任务 `uiCoreTest uiKitTest verifyUiJava8Bytecode`：JUnit `414/414` 通过，389 个生产 class 均验证为 Java 8 major 52。
- 第一轮目标 `compileJava` 耗时 1 分 42 秒，前端仅暴露 13 个 `sealed/permits`、模式 switch 与接口私有方法错误；对应语法门槛已按批次清除。
- 第二轮目标 `compileJava` 耗时 16 秒，已进入真实版本 API 符号层。javac 达到默认 100 错误上限，首批错误集中于公开 `api` 包的现代类型映射（`ServerPlayer`、`world.item.ItemStack/Item`、`Fluid`、`Direction`、`BlockPos`），不是随机分散的构建故障。下一批按完整包迁移公开 API，同时并行本地化 `server/data` 的 NBT 与原子持久化。
- 当前目标生产源码为 819 个 Java 文件；没有源集排除、永久空桩或删除功能来伪造绿色。构建仍为预期红色，尚未达到可发布或可运行 RTSBuilding 的状态。

### 2026-07-28 第一轮代码落地补记

- Forge 生命周期首批：`@Mod` 入口、1.12 事件总线注册、物品/方块/实体/创造栏、配置与相机实体已改写到 Forge 14.23.5.2847 语义；配置层独立 Java 8 编译通过。
- 物品首批：`RtsPluginItem` 已成为真实 1.12 `Item`，具备无参注册构造、服务端权威的右键安装、`ActionResult`、本地化 tooltip 与客户端 Ctrl 展开提示；使用真实 Forge/MCP classpath 的 Java 8 编译通过。服务层的 `installHeldPlugin(EntityPlayerMP, EnumHand)` 对接正在继续。
- 网络首批：已建立 `SimpleNetworkWrapper` 单通道、显式 discriminator 与重复编号校验；相机域 4 条消息已完整迁为 Java 8 `IMessage`，编码往返 `4/4` 通过，非法 NaN/越界输入拒绝 `1/1` 通过。
- 网络第二批：删除工作流、保护工作流、撤销共 3 条服务端权威消息已迁移，编码往返 `3/3` 通过，非法负 ID 拒绝 `2/2` 通过；当前共有 7 条消息进入真实 1.12 网络实现。服务端尚未本地化的精确接口暂用“精确类名 + 精确签名 + 找不到即显式失败”的隔离反射，未吞错、未假成功。
- 服务端 task/history/pipeline 首批：完成 task ID、提交 ID、等待键、快照、墓碑、slice/result、pipeline result/key 等 Java 8 数据结构改写；新增维度 ID 与 NBT 兼容边界，三个任务 state/codec 和 history 已开始使用真实 1.12 `NBTTagCompound`、`BlockPos`、`EnumFacing`、`IBlockState`、`WorldServer` 语义。18 个可隔离纯类 `javac --release 8` 通过。
- 构建环境下载异常处理：首次模板 `build` 在 Maven Central 的 Netty `4.1.9.Final` 下载连接中长期停滞；只终止了经父子关系和命令行确认的模板 Gradle 进程，随后用同一 Maven Central 地址下载并按 SHA-1 `0097860965d6a0a6b98e7f569f3f966727b8db75` 写入专用 Gradle 缓存，再次启动模板构建。没有停止或修改其他 Java/Gradle 进程。
- 阶段 0 构建已绿色：ForgeDevEnv 空白模板 `build` 在补齐 Java 21 JST toolchain 后成功，`18 actionable tasks`，最终 `BUILD SUCCESSFUL in 59s`。模组样例 class 已核验为 Java 8 `major version 52`。
- 阶段 0 专用服务端烟测已绿色：Forge `14.23.5.2847` 在 Azul Java `8u502` 上识别并成功加载 6 个模组（含样例模组），创建三维世界并到达 `Done`；随后通过服务端 `stop` 命令正常保存世界并退出，Gradle `runServer` 为 `BUILD SUCCESSFUL in 39s`。
- 阶段 0 客户端烟测已绿色：首次完整下载 1.12.2 的 `1305/1305` 资源，LWJGL `2.9.4`、Intel OpenGL `4.6` 初始化成功，Forge 识别并成功加载 6 个模组、声音与纹理图集初始化完成并到达主菜单；随后终止本次明确的模板客户端测试任务，无残留模板 Java 进程。
- 蓝图公共层已全量迁移：`common/blueprint` 13 个原文件全部改为 Java 8/真实 1.12 API，Vanilla structure、Sponge、Litematic、Building Gadgets 四类读取能力保留；方块状态改用显式 `Name/Properties` NBT。真实 MCP 整包定向编译通过，压缩/内存 NBT、属性状态、tile NBT、缺失方块与生存净化共 10 项测试通过。
- 插件域已全量迁移：catalog、registry、安装状态、持久化、耐久、队伍共享、旧技能树迁移、服务与物品均进入 Java 8/Forge 1.12 语义；保留完整真实 `ItemStack`/NBT，并修复 1.12 背包“部分插入后复制”的风险。核心逻辑检查 `3/3` 通过。
- 网络累计已有 33 条显式消息迁入真实 `SimpleNetworkWrapper`；覆盖相机、工作流、蓝图上传/状态/恢复、放置控制、储存绑定、快速丢弃、破坏、流体、寻路、挖掘/区域破坏、单放置/批量放置/流体放置/远程交互。discriminator 全唯一；各批 Java 8 编解码、完整 ItemStack/NBT 往返及恶意边界测试均通过，现代网络 API 残留文件由 100 降至 67。
- 公开 `api` 包已全量本地化：11 个文件全部检查，10 个版本相关文件改为 `EntityPlayerMP`、1.12 `Item/ItemStack`、Forge `Fluid`、`EnumFacing`、`BlockPos` 与真实 `BuilderMode`；临时 `Object/List<Object>` 坐标已收紧，和网络分派签名一致。整个包使用真实 Forge stable_39 classpath 的 `javac --release 8` 通过。
- `server/api` 实现层已同步上述公开签名：10 个实现保留原委托方法、参数顺序、默认值、返回值和异常路径，清除 Object 坐标、pattern matching 与 `Stream.toList()`；当前交叉前沿已推进到 `server/service/api` 服务接口及两个直接服务方法。
- `common/shape` 已完整迁为 1.12 几何与世界类型：12 个生产文件定向 `javac --release 8` 通过；`AreaShapeInput` 与圆柱 Cell 已从 record 明确降为保留值语义的 Java 8 类；原有形状契约测试同步后 `7/7` 通过，覆盖注册序数、直线顺序、矩形/盒子空心边界、圆柱高度与圆足迹一致性、球体体积。
- `server/data` 第一轮已完成：NBT、压缩读写、世界存档路径、`WorldSavedData/MapStorage` 与整数维度均落到 1.12；Found/Missing/Failed、严格原子替换、备份与错误传播保持。`PlacedBlockTrackerData` 使用 `NBTTagList<NBTTagLong>` 保存完整 long，兼容旧键并对损坏元素显式报错；包含 long 极值和超 32 位值的往返通过。
- `server/service/api` 12 个接口均完成 1.12 类型同步，其中 9 个无外部会话依赖的接口真实 ForgeBin 定向编译通过；`common/placement` 两个状态转换器也已迁为 `IBlockState/IProperty/EnumFacing/Rotation/BlockSlab`，保留方块自身旋转优先级、属性白名单和网络字符/长度上限，定向编译通过、隔离断言 `6/6`。
- 第三轮整包 `compileJava` 在 21 秒内进入客户端平台层并达到新设的 1000 错误上限；错误前沿由 `client/bootstrap`、按键、相机、GUI/standalone 的现代 NeoForge、GLFW、GuiGraphics 与现代客户端类型占满。此轮没有回退到先前已清除的公开 API/服务端数据语法前沿；下一阶段以客户端入口、输入和 Minecraft Canvas/GUI 适配器为主战场。
- 首个本地机械抢滩检查点已提交：`2482d03c`（314 文件，14351 insertions / 8200 deletions）。提交后已强制执行 `git show --stat --oneline HEAD -1`，边界与预期一致；分支仅本地领先 1，未 push。
- 客户端状态/声音小闭环：`RtsClientUiStateStore`、`UiStateCache`、`UiStateCodec` 已换为 Forge 1.12 配置目录、Log4j2 与 Java 8 GZip 读取，三文件定向编译通过，声音设置兼容测试 `3/3`；方块声音消息已改为 `IMessage`，discriminator 165，当前 38 个注册编号全部唯一，消息往返 `1/1`。客户端以 1.12 `PositionedSoundRecord`、BLOCKS 分类和无距离衰减播放，定向编译通过；公共消息处理器仅在客户端调度后按字符串加载声音类。

### 2026-07-28 客户端第二波抢滩

- 客户端生命周期已经真正接入共同入口：`RtsbuildingMod` 仅在物理客户端通过字符串边界调用 `RtsClientBootstrap`，专用服务端解析入口类时不解析 client 类型；bootstrap 负责 Forge 1.12 客户端事件、30 个按键和本地入门提醒命令。`mcmod.info` 的 `guiFactory` 同步指向该 bootstrap。
- 相机入口和同步改为 1.12 `EntityRenderer`/RenderTick；相机平滑、运动边界、灵敏度和视觉插值数学已本地化，定向编译通过，隔离边界断言 `3/3`。
- `client/pathfinding` 五文件已完整迁移并通过真实 ForgeBin Java 8 定向编译。步行、创造飞行、鞘翅、游泳、保守爬行检测、方块摩擦、液体减速、真实碰撞表面、卡住恢复、精确降落、网络跟踪和第三方移动模式事件均保留；值语义、不可变高亮快照和优先级测试通过。
- `client/input` 与 `client/network` 已清除 NeoForge/GLFW3/现代 Screen API：使用 Forge ClientTick、GuiScreenEvent、LWJGL2 和 `SimpleNetworkWrapper`，保留松开触发、held/clicked 区分、拖拽、滚轮消费、搜索输入、容器 carried stack 和关闭 GUI 后原生鼠标恢复。新增 `LegacyGuiGraphics` 统一封装 1.12 静态矩形、无阴影文字、物品、tooltip 与矩阵缩放；scope 现代残留扫描为零。
- 远程菜单与 GUI 自动探针已迁为 1.12 `Container/GuiContainer/RayTraceResult/ClientCommandHandler`。只对受支持且明确标记的同一远程 windowId 放宽距离；Sophisticated 类屏幕仍执行具体容器安全配对。自动探针继续记录转场 TSV、screenless 状态、setup、稳定 tick、超时和 auto-exit；命令归一化、TSV、容器识别、库存包装和 windowId 纯逻辑检查通过。
- 入门提醒作用域继续按单人世界目录或服务器地址隔离，Java 8 兼容测试 `4/4`；背包插件按钮和 Shift 安装入口、聊天区避让、插件显示 catalog、创造栏搜索缓存、快捷槽/远程绑定状态与储存页解码已进入 1.12 API。储存解码使用 Forge Item/Fluid registry、真实 `ItemStack` 副本和不可变快照，定向编译通过。
- 曾检测到两个由同一小 agent 误启动的完整 Gradle 编译。核验进程树后确认其中一个使用了错误的 wrapper/cache，另一个输出会话已丢失；已只终止这两个明确指向目标工作树的进程。后续整包 `compileJava` 统一由协调 agent 使用既定 Gradle 9.2.1、专用缓存和 toolchain 串行执行，避免把不可审计输出记作验证结果。
- 第四轮真实 Gradle 9.2.1 构建由资源验证请求触发（模板的 `processResources` 依赖主 classes 链），54 秒后在 `compileJava` 达到 1000 错误上限。错误首前沿已从 bootstrap/input 移到 `client/controller`，后半段进入 `client/rendering` 的 PoseStack/VertexConsumer/现代世界渲染 API；这轮因此不能记作资源绿色，但明确证明前两轮客户端入口墙已越过。编译时两个受控小批次仍在写各自独立的 service/network 包，故该账只用于选择下一战场；收束后必须再跑一次静态源树上的串行整包编译。
- 插件/合成网络域新增 11 条真实 `IMessage`：plugin 68–71、craft 72–78，全部具备无参构造、Java 8 codec、边界验证和显式方向。C2S 在服务端线程交给 `EntityPlayerMP` 权威服务，S2C 通过客户端线程和字符串类边界分派；11/11 codec 往返通过，固定 9 格、复杂列表和 ItemStack/NBT 均覆盖，六类恶意超长/超量输入均被拒绝。全仓当前 49 个 discriminator 全唯一，新域方向为 server 8 / client 3；真实 ForgeBin 定向编译通过。
- 客户端 service 版本相关层已全量过第一轮：CameraInputSampler/Orbit、BuildPlacement、MiningOperation 及相机数学/视角恢复均改为 1.12 Minecraft、EntityPlayerSP、LWJGL2、RayTraceResult/Vec3d、IBlockState/ItemBlock、ForgeRegistries 和原版渲染视角 API。相机镜像生命周期、心跳同步、放置/交互意图、区域挖掘边界、选择状态与自动清除均保留；四个主服务连同相机辅助类真实 ForgeBin Java 8 定向编译通过，区域正负方向、36 格尺寸、±35 高度和阶段清理边界通过。运行测试后半因测试 classpath 的 Guava 哈希路径写错停止，已明确归类为测试启动依赖，不记作业务绿色也不继续追冷依赖。
- `client/controller` 已完成第一轮 1.12 收口：GUI 切换、移动输入、创造权限、当前容器/windowId、死亡交接与远程菜单关闭均改成 1.12 路径；储存页/合成/进度状态使用 1.12 registry、Forge Fluid 和 Java 8 显式不可变值对象。点名现代 API/语法扫描为零，`git diff --check` 通过；状态类使用真实 ForgeBin `--release 8` 定向编译通过。全 controller 定向编译仍被包外尚未迁移的 feedback/progression/builder payload 与真实合成终端 Screen 阻断，因此不记作全包绿色。
- 静态源树上的第五轮串行 `compileJava` 使用 Gradle 9.2.1、RFG 2.0.2、专用缓存、Gradle JVM JDK 25.0.3、JST JDK 21、编译 JDK 17 与 Java 8 发布 toolchain，20 秒后达到 javac 1000 错误上限。首错误已经前移到尚未迁移的 `network/feedback`、`network/progression` 和剩余 `network/builder` S2C payload；随后集中于 `client/screen/culling`、`client/screen/blueprint` 与选择/渲染平台层。此次在所有写入 agent 停笔后运行，可作为当前可复验前线；构建仍红，1 个警告来自既有过时 API，不能启动 RTSBuilding 客户端或服务端。
- 第五轮之前的树级 `git diff --check` 通过，仅有 Windows LF/CRLF 提示；`client/controller` 与本批五个 `BlueprintGhost*` 文件的现代类型扫描已清除，未纳入该批的 `BlueprintCaptureRenderer` 仍保留 PoseStack/VertexConsumer/现代 BlockPos/AABB，已明确列入下一渲染批次，没有通过排除源码掩盖。
- 蓝图幽灵渲染五文件已完成真实 1.12 渲染边界：使用 IBlockState、BlockRendererDispatcher/IBakedModel、按 BlockRenderLayer 烘焙，以及由本模组独占并自行上传的 BufferBuilder；调用方兼容参数不再被读取、结束或污染，fallback/包络线也不触碰 Minecraft 共享缓冲。ForgeBin + LWJGL2 Java 8 定向编译通过，边界与顶点纯测试通过；动态 TileEntity 预览因现有 BlueprintGhostBlock 不携带 TE/NBT 被明确隔离，未伪造支持。
- `client/screen/culling` 的四个几何/射线核心已迁为 1.12 BlockPos、Vec3d、AxisAlignedBB、EnumFacing 与 RayTraceResult，record 均降为保留同名访问器和值语义的 Java 8 不可变类。ForgeBin + Guava 21 定向编译通过；闭区间归一化、六向缩放、256 边界、ray slab 平行 miss、非单位方向、手柄筛选/拖拽释放、剔除块后继续 clip 与零方向 guard 的纯 harness 通过。其余 Manager/Panel/ClientState/WorldInput 正作为下一批继续。
- 服务端生存进度四文件及统一 S2C 出口已进入 1.12 边界：玩家、队伍、世界、NBT、BlockPos、整数维度与 SimpleNetworkWrapper IMessage 发送均完成映射；家园 selection、3×3 开启范围、20 游戏日迁移冷却、个人/队伍共享持久化和登录迁移语义保留。使用真实 ForgeBin/Netty、对外依赖最小签名 stub 的 `javac --release 8` 定向编译通过；stub 位于忽略的 build 验证目录，不进入生产或提交。
- 第二个本地机械抢滩检查点已提交并强制核验：`73ac8babe95cf05ec6805914bc7ab287048293c7`（99 文件，4448 insertions / 3861 deletions）。该提交有意排除了当时正在写的 progression/feedback、screen/culling 与 `RtsPayloadRegistrar`；分支仅本地领先 2，未 push。
- feedback/progression 网络域共 8 条消息已转为 Java 8 `IMessage`，固定 ID 为 4、8–14；C2S 全部回到 WorldServer 主线程，管理员开关在执行点复核权限，S2C 继续通过字符串客户端边界分派。全消息真实 Netty ByteBuf 往返通过，负数/NaN damage、非法 quest mode/phase、过长维度、负 cooldown 与超大任务计数均被归一化或拒绝；全网 54 个注册编号全唯一，现代网络 API/语法扫描为零。
- 服务端相机会话两文件已迁为 1.12 EntityPlayerMP、WorldServer、Entity/相机实体、Vec3d、MathHelper 与状态栏文本；跨维度相机查找、孤儿/所有者清理、家园选择、锚点跟随、正方形动作范围、旋转/俯仰/高度/滚轮边界、丢失实体恢复以及启停同步语义保留。真实 ForgeBin/Netty、严格外部签名 stub 的 Java 8 定向编译通过；生产代码没有 stub 或反射降级。
- culling 客户端九文件与网络七文件已形成 1.12 闭环：三阶段草稿/确认/保存、剔除/揭示、六向手柄 resize、恢复 ID 归一化、世界输入路由、维度校验与有界 codec 均保留。三条真实消息注册为 79 request SERVER、80 save SERVER、81 state CLIENT；codec 恶意计数、Manager 状态机和 ForgeBin Java 8 定向编译通过。服务端 `RtsCullingPersistence` 仍未迁，handler 对预期精确签名显式失败而不吞错；该服务端类已列为后续硬前线。
- 总网络注册器已接入 culling，静态审计结果为 `REGISTERED=57 UNIQUE=57 / NO_DUPLICATES`；本批相关目录现代 MC/NeoForge/GLFW3/record 残留为零，树级 `git diff --check` 通过（仅换行提示）。
- 第六轮静态树串行 `compileJava` 使用与第五轮相同的受控 toolchain，20 秒后达到 1000 错误上限。首错误已继续前移到 `network/builder` 剩余 12 个现代 payload（挖掘/ultimine/工作流进度/恢复扫描/动画/历史等）；随后主要是 `client/screen/blueprint`、`canvas/MinecraftUiCanvas` 与 standalone/panel 的 GuiGraphics/Font/Screen/GLFW3 平台墙。先前 feedback/progression/culling/camera 前线没有回退。构建仍红，1 个既有过时 API 警告，不能启动 RTSBuilding 运行时。
- 第三个本地检查点已提交并强制核验：`ea08d403`（35 文件，1449 insertions / 1103 deletions），包含进度、相机、feedback/progression 网络和 culling 客户端/网络闭环；当时正在写的总 registrar、builder 与 GUI 地基被有意排除。分支仅本地领先 3，未 push。
- `server/culling/RtsCullingPersistence` 已补齐 1.12 精确签名，关闭 culling handler 的显式反射缺口：按玩家与整数维度隔离，盒子最多 128、揭示方块最多 4096；揭示坐标使用 1.12 可用的 `NBTTagList<NBTTagLong>`，避免不存在的 long-array API。ForgeBin Java 8 编译通过，归一化、长坐标、不可变快照与维度隔离 `5/5` 通过。
- GUI 基层五文件已迁为 `LegacyGuiGraphics`、FontRenderer、GuiTextField、GuiButton、ITextComponent 与 LWJGL2：Canvas 嵌套裁剪/矩阵，按钮/滑杆状态，文本框 placeholder/read-only/responder/scroll-end/内边距一致性，以及窗口 z-order/拖动/缩放/吸附/pointer capture/scissor 均保留。真实 ForgeBin + UI Core/Kit Java 8 定向编译与 `GuiFoundationCheck` 通过；光标/选区与高缩放最终画面仍列为人工验证，不阻塞继续迁移。
- builder 剩余 12 个现代 payload 已全部迁为 Java 8 `IMessage`，新增 1.12 C2S handler、九类 S2C 客户端隔离分派和注册 facade，固定 ID 16–27。真实 Netty 覆盖 12 类往返、批量/缺失物品/字符串恶意边界，结果 `builder-codec-roundtrip-ok`；builder 现代 payload 残留为零，全网更新为 `REGISTERED=69 UNIQUE=69 / NO_DUPLICATES`。三个 C2S handler 指向的 server workflow/service 仍在后续版本本地化前线，当前用精确签名显式失败边界，不记作运行态绿色。
- `RtsSelectionBoxAnimator` 已迁为 AxisAlignedBB/MathHelper 与 Java 8 手写 lerp，和刚完成的 culling box 使用同一 1.12 几何类型；ForgeBin + Guava 21 定向编译通过。
- GUI 承重层又完成四文件：`RtsPanel` 使用 LegacyGuiGraphics；FloatingWindow layer/router 保留窗口 z-order、最上层遮挡、pointer capture、keyboard focus、Escape 栈和窗口内滚轮 BLOCK；BottomBar adapter 保留完整 transition 与 ItemStack metadata/NBT 等价性。真实 ForgeBin/UI Core 定向编译通过，`GuiAdapterCheck` 覆盖重叠窗口、越界拖放、窗口外滚轮、Escape 和搜索 transition。
- storage 11 条剩余 C2S 消息与三个 handler 已迁为 Java 8 `IMessage`/服务端主线程，固定 ID 35–45；槽位、windowId、数量、字符串、face、BlockPos 和真实 ItemStack metadata/NBT 均有边界，传输服务继续接收真实 prototype 并承担 mutation remainder/refund。codec 边界测试输出 `storage-codec-boundary-ok`；全网为 `REGISTERED=80 UNIQUE=80 / NO_DUPLICATES`。旧 `RtsStoragePackets` 的 NeoForge registrar 已改为三个 1.12 子域的单一聚合入口。
- 蓝图小数据层 `BlueprintGhostBlock`、`BlueprintGhostPreview` 与 `BlueprintCaptureGeometry` 已改为 1.12 IBlockState/BlockPos 和保留 record 访问器、equals/hashCode/toString 的 Java 8 不可变类；ForgeBin 定向编译通过。
- 第七轮静态树串行 `compileJava` 耗时 21 秒，仍在 1000 错误上限结束，但首错误已从全部网络域前移到 `client/rendering/animation` 五文件（fake-air、pending/confirmed/destroy/placement 动画的现代 ClientLevel/PoseStack/VertexConsumer）；其后仍是 standalone BuilderScreen 家族。已迁 GUI 基层和 storage 网络没有回退，证明 80 条消息及 Canvas/Window 基础已越过前端。构建仍红，尚不可启动本模组运行时。
- 第四个本地检查点已提交并强制核验：`58dfb643b49d8e437ff73e12c1208ed26a3ba6cc`（47 文件，1978 insertions / 1881 deletions），包含 builder/storage 网络闭环、GUI 基层与承重层、culling 持久化、选择框插值和蓝图小数据层；提交边界有意排除当时仍由两个小 agent 写入的 animation/selection 渲染目录。分支仅本地领先 4，未 push；源基线仍为 `origin/main` 的 `d578c13101ccde6bf572d61fc7f52be48788a2e6`。
- 动画渲染五文件已迁为 Forge 1.12.2：fake-air 的服务端状态等待与 750ms 恢复窗口、pending 的 5s 生命周期和 220ms grow、confirmed/destroy 的 220ms 动画、bounds 裁剪、模型/填充/线框均保留。模型、填充和线框分别使用本模组独占 `BufferBuilder` 与 uploader，兼容入口不读取或结束调用方/Tessellator 共享缓冲，GL 状态由 `finally` 恢复。最终 Forge 14.23.5.2847 patchedMc/unpacked classpath 与额外 14.23.5.2768 ForgeBin 均以 Java 8 定向编译通过；`AnimationTimingCheck PASS` 覆盖动画端点、5000/5001ms 清理边界、确认清 pending、12 边线框和 6 面填充。
- 两个 selection 世界渲染器已迁为 1.12 EnumFacing/AxisAlignedBB/Vec3d/RenderGlobal/LWJGL2；六向轴色、hover 外扩与提亮、active 金色、旋转 ribbon/菱形箭头语义保留。两类渲染各自拥有私有 fill/line buffer，GL 状态精确恢复；真实几何检查输出 `selection-render-geometry-ok`，覆盖六向 480/288、仅 UP/DOWN 120/72、旋转弧 12/16 顶点以及普通/hover 半宽 0.072/0.105。ForgeBin Java 8 定向编译、现代 API/共享缓冲生命周期扫描和 `git diff --check` 均通过；真实方块模型、GPU 透明排序和最终视觉仍留到运行态/人工画面验证，不阻塞后续机器移植。
- 蓝图捕获与 culling 世界渲染三文件已迁到 1.12，并新增 `RtsCullingWorldRenderBridge` 接入 `RenderWorldLastEvent`。包含块、双层排除块、捕获边界、剔除盒 hover/selected/draft 与六向手柄语义保留；全部使用独占 `BufferBuilder + WorldVertexBufferUploader`，不触碰 Tessellator/调用方共享缓冲，并完整恢复 GL 状态。Forge 14.23.5.2847 patchedMc/unpacked 定向编译通过，三个 class 均为 major 52；几何检查覆盖剔除盒 30/18、捕获填充 90/边框 36 顶点以及 0.01 外扩。大捕获区域使用 8 MiB 私有填充缓冲以覆盖现有 8192 包含块与 1024 双层排除块上限。未来总渲染器完成迁移后必须避免该临时桥与总入口重复注册/重复绘制；透明度、线宽及第三方事件叠放顺序列入运行态人工验证。
- overlay 六文件已完成 1.12 审阅与移植：Boundary 力场纹理/滚动 UV/高度扫描、3×3 ChunkGuide、交互目标的方块/实体择近与剔除穿透/命中面雾层/UI 阻断、玩家移动目标淡出、双箱储存合并及绑定颜色过渡均保留；`InteractionTargetSelection` 本就是纯 Java 8，审阅后不改。五个版本相关渲染器均使用私有 BufferBuilder/uploader，兼容入口不触碰共享缓冲，GL 状态由 finally 恢复。14.23.5.2847 覆盖层 + stable_39 ForgeBin/LWJGL2 的 Java 8 定向编译通过，几何与边界契约 `10/10 PASS`，现代 API/共享 Tessellator 扫描 `CLEAN`，`git diff --check` 通过。力场纹理方向、远距离透明度、自定义相机射线、浮窗遮挡和双箱合并画面列为人工项。
- standalone 输入路由五文件已审阅，仅两个存在版本依赖并完成改写：键码使用 LWJGL2 `Keyboard`，Escape、左右/小键盘 4/6 与 Alt+Space 消费保持；modifiers 继续接受兼容位掩码，且在 1.12 上层暂传 0 时由左右 Alt 实时状态兜底。鼠标左右键映射为 1.12 `GuiScreen` 的 0/1 编号，轮盘独占和点击层级不变；PrimaryActionHost/ScrollRouter/InputHost 为纯逻辑无需修改。真实 ForgeBin/LWJGL2 Java 8 定向编译通过，路由顺序、轮盘独占、Alt+Space 传播、点击和滚轮防泄漏输出 `input-route-order-and-wheel-consumption-ok`。
- 第八轮静态树串行 `compileJava` 使用既定 Gradle 9.2.1、RFG 2.0.2、专用缓存和 Java 8 发布 toolchain，20 秒后达到 1000 错误上限。首错误已从 animation 前移到 `client/rendering/builder` 的 12 个现代渲染/状态解析文件；其后是 `client/screen/shape` 选择状态机与 mode 的放置/旋转轮盘。刚完成的 animation、capture/culling、overlay、selection 没有回退。构建仍红，1 个警告为既有过时 API；下一批已将 builder rendering 与 screen/shape 分为两个不重叠战区继续迁移。
- 第五个本地检查点已提交并强制核验：`f74bfe632fff6e4aab83ffef8096d6e622f573d8`（18 文件，2216 insertions / 1997 deletions），包含 animation、capture/culling、overlay、selection 世界渲染和 standalone 输入路由；提交时 builder rendering 与 screen/shape 尚未开始写入，边界干净。分支仅本地领先 5，未 push。
- `client/rendering/builder` 12 文件已完整进入 1.12：ItemBlock 真实放置状态、四向旋转与预设，多方块门/双层植物/床模型，刷怪蛋/末影水晶临时实体预览，build/destroy/ultimine、合并骨架缓存/增量删除/8 帧存活复查、16384 edge/768 fill 上限，以及普通/高级选择手柄均保留。所有模型/fill/line 使用私有 BufferBuilder/uploader，调用方 buffer 仅作 ABI 兼容且生命周期扫描为零；GL 状态快照在 finally 恢复。经行数骤减复核后补回了 MergedSkeleton 复查队列/边上限和实体括号竖边，未以压缩代码静默删功能。14.23.5.2847 + stable39 定向 Java 8 编译通过，几何/yaw/半开边界 `8/8 PASS`，现代 API 与 `git diff --check` 干净。1.12 实体 alpha 使用 GL blend/color 调制，第三方实体材质是否尊重透明度列为人工验证。
- `client/screen/shape` 19 类全检、17 类改写完成：两点/三点/高度/box/line、负方向、射线平面交点、大小/体积钳制、READY 状态机、预览过滤和世界操作规划均迁为 1.12 几何/世界类型；record/缓存键改为具备 equals/hashCode 的 Java 8 不可变类。14.23.5.2847 universal 覆盖层 + stable_39 ForgeBin 定向编译通过，major 52，行为结果 `SHAPE_HARNESS_5_5_OK`；现代 API/record/List.of 无真实残留。外围 BuilderScreen/Constants、InteractionTypes、RtsWorkflowStatus 仍由各自批次负责，shape 对当前精确签名已核对。
- 放置方块旋转手势两文件已迁为 EnumFacing/World/IBlockState/AxisAlignedBB/Vec3d 与 LWJGL2；左右绕 Y、上下绕 camera-right、正负 quarter-turn、目标方块/区块失效、四条 15 采样点弧、共用采样点的射线 hover/click 及 ArcHandle 值语义保留，并与已迁 selection renderer 访问器对齐。14.23.5.2847 + stable39 定向 Java 8 编译通过（仅既有 deprecated warning），行为输出 `rotation-mode-behavior-ok`，现代 API/GLFW3 无残留。
- 第九轮静态树串行 `compileJava` 使用相同受控 toolchain，21 秒后达到 1000 错误上限。builder rendering 与 screen/shape 两堵墙均已消失，首错误推进到总渲染入口 `RtsVisualOverlayRenderer`，随后是 `client/rendering/util` 六文件，再后进入蓝图库 UI 的 Font/GuiGraphics/registry/TinyFileDialogs/现代网络路径。构建仍红，1 个警告来自既有过时 API；下一批按总渲染入口+util 与蓝图 UI 分离推进。
- 第六个本地检查点已提交并强制核验：`753f5e9b7dbc55f4326ae0d39c38b99940c24e2d`（32 文件，1273 insertions / 2883 deletions），包含 builder rendering、screen/shape 和旋转手势两文件及第九轮编译记录；分支仅本地领先 6，未 push。仓库根三个 `javac.20260728_04*.args` 在最后一个 agent 首次编译前已经存在且来源无法可靠归属，按脏树纪律保留为未跟踪噪音，不纳入提交。
- 总渲染入口与 util 已完成 1.12 闭环：`RtsVisualOverlayRenderer` 使用 `RenderWorldLastEvent` 按固定 11 步分派全部已迁 renderer，`RtsClientModEvents` 在幂等注册边界内显式注册该 class；旧 `RtsCullingWorldRenderBridge` 取消订阅，避免 culling 双绘。角框、alpha、私有 BLOCK 模型、边界、光标射线和 ray freeze 均使用 1.12/LWJGL2 路径；新增几何只用私有 BufferBuilder，GL 状态 finally 恢复。定向 Java 8 编译及 major 52 通过，`RENDER_UTIL_CONTRACT_5_5_OK`；注册/handler 唯一性 1/1、分派顺序 11/11、旧桥 inactive，现代 API/共享 Tessellator 可执行引用为零。特别修复了“只有 @SubscribeEvent 但从未注册到 1.12 EVENT_BUS”会导致全套渲染完全不执行的生命周期缺口。
- blueprint 核心/文件/会话 14 文件已迁入 1.12：捕获状态机与分帧扫描、真实 TileEntity NBT/metadata/blockstate、同目录临时文件+原子替换、库扫描/解析/搜索/导入导出/同步/删除/重命名、旋转默认值、固定锚和相对镜头微调、上传放置网络意图、Forge item/fluid registry 材料统计均保留。TinyFD 替换为 EDT `JFileChooser/JOptionPane`，无头或无 Desktop 显式报错；所有读写先做绝对规范化和蓝图目录边界检查，拒绝路径穿越，名称清洗覆盖斜杠、纯标点、Windows 保留名和长度。13 个真实依赖文件与 UiStateAdapter 精确外围签名均定向 Java 8 编译通过，现代 API 扫描、路径边界、原子写入、异步错误、网络意图、捕获状态、1.12 流体和无头选择器契约通过。综合 harness 启动 Minecraft registry 时缺独立运行的 LaunchWrapper/Commons Lang，未把该测试环境缺口误记为业务绿色或生产失败。
- blueprint UI 12 文件已迁为 LegacyGuiGraphics/MinecraftUiCanvas/FontRenderer/ITextComponent 与 1.12 世界输入，捕获、预览、材料、命名、搜索、滚动、按钮和浮窗层级完整保留。命名 Enter/Backspace/字符、modal Escape、主窗文本框聚焦/失焦提交、捕获/材料/库内滚轮全消费、空库/空材料、长文案像素裁切、Canvas scissor 及全局无阴影文字均有契约覆盖；命名/材料窗直接使用 Minecraft.fontRenderer，避免依赖尚未迁移的 BuilderScreen 字体接口。14.23.5.2847 + stable_39 定向 Java 8 编译通过，蓝图 UI Core/Kit `42/42`、自定义契约 `11/11` 通过，现代 API 扫描与 `git diff --check` 干净。
