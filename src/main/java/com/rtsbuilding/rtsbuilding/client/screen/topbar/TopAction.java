package com.rtsbuilding.rtsbuilding.client.screen.topbar;

/**
 * 顶部栏当前动作枚举。
 * <p>
 * 表示当前控制器所处的模式对应的操作类型，
 * 用于顶部栏按钮的高亮显示。
 */
public enum TopAction {
    INTERACT,
    LINK,
    FUNNEL,
    ROTATE
}
